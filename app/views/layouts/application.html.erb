<!DOCTYPE html>
<html>
<head>
  <title>ProductivityRPG</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>

  <ul>
    <% if flash[:errors] %>
      <% flash[:errors].each do |error| %>
        <li>
          <%= error %>
        </li>
      <% end %>
    <% end %>
  </ul>

  <div class="container">
    <% if signed_in? %>
      <span class="welcome">Greetings, <%= current_user.username %>!</span>
    <% end %>
    <%= yield %>
  </div>

</body>
</html>
