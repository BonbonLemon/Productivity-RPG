<!DOCTYPE html>
<html>
<head>
  <title>ProductivityRPG</title>
  <link rel="stylesheet" media="screen" href="https://fontlibrary.org/face/pixelated" type="text/css"/>
  <link href='http://fonts.googleapis.com/css?family=Lato&subset=latin,latin-ext' rel='stylesheet' type='text/css'>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>

  <ul>
    <% if flash[:errors] %>
      <% flash[:errors].each do |error| %>
        <li>
          <%= error %>
        </li>
      <% end %>
    <% end %>
  </ul>

  <div class="container-fluid">
    <p>&#xf014;</p>
    <% if signed_in? %>
      <span class="welcome">Greetings, <%= current_user.username %>!</span>
    <% end %>
    <%= yield %>
  </div>

  <footer class="container-fluid">
    <div class="col-xs-4">
      <a href="https://github.com/BonbonLemon/Productivity-RPG">GitHub Repo</a>
    </div>
    <div class="col-xs-4">
      <a href="#">About the Creator</a>
    <div>
  </footer>
</body>
</html>
